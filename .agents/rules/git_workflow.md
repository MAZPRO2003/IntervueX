# Git Branching Rule

- **DO NOT commit or push directly to the `main` branch under any circumstances.**
- Always create a descriptive feature branch (e.g. `fix/gradle-memory-limits` or `feature/xyz`).
- Commit changes and push the feature branch to `origin`.
- Notify the user so they can review and merge the branch into `main`.
